package adventofcode;

import java.util.ArrayList;

public class Part2 {
    public static void solve(String inputFilePath) throws Exception {
        ArrayList<String> logs = Util.readLogFile(inputFilePath);

        System.out.println(String.format("The sleep peak minute of the most predictable guard (#%d) was 00:%02d. Thus, the answer to part 2 of the puzzle is: %d", 0, 0, 0 * 0));
    }
}
